'use strict';

xdescribe('view2 module', function() {

  beforeEach(module('view2'));

  describe('view2 controller', function(){

    xit('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});